import { domMax } from 'framer-motion';

// Animation features (including layout animations for the sliding pills),
// split out so they load after the page instead of in the first bundle.
export default domMax;
